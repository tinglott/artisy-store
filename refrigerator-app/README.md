# Fridge / Recipe
A lightweight refrigerator-to-recipe PWA-style web app. Add ingredients, see recipe matches, and keep inventory in the browser.

## Run locally
Open `index.html` or serve this folder with any static server.

## Deploy
This is Netlify-ready: publish the repository root (or the `refrigerator-app` folder if using a monorepo).
